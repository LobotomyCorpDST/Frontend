name: Docker Image CI

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:

  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4
    - name: go Folder
      run: cd app
    - name: Build the Docker image
      run: docker build --file Dockerfile -t kanyanatuet/backend-test:latest .
    - name: login
      run: docker login -u kanyanatuet -p dckr_pat_X_ubcwpGPWIfXhjzeBOcDuy48iY
    - name: push docker
      run: docker push kanyanatuet/backend-test:latest
